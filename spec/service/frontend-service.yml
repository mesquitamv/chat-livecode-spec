---
apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: livecode-stg
  labels:
    app: frontend
spec:
  ports:
    - port: 3000
      targetPort: 3000
      protocol: TCP
      name: http-port
  selector:
    app: frontend